## Summary

Closes #{{ISSUE_NUMBER}}.

This PR was opened by `bin/auto_dev_loop.sh` and merged with `--admin --squash` after local verification passed. See driver wake log `logs/auto_dev_{{WAKE_TS}}.log` for details.

## 验收

- [x] 本地 `uv run pytest -q -m "not live"` 通过
- [x] 本地 `uv run lint-imports` 通过
- [x] 本地 `uv run ruff check . && uv run ruff format --check .` 通过
- [x] 本地 `uv run mypy src/copy_trader` 通过
- [x] `check_subagent_diff_safe()` 通过（无禁区文件 + 无凭证泄露）
- [x] 替换 base 后再次跑本地校验仍通过

## Spec / Capability

详见对应 issue body 中链接的 `openspec/specs/<capability>/spec.md`。

## Driver 元信息

- Wake timestamp: `{{WAKE_TS}}`
- Wake log: `logs/auto_dev_{{WAKE_TS}}.log`
- Subagent log: `logs/auto_dev_subagent_{{ISSUE_NUMBER}}_{{WAKE_TS}}.log`
- Driver state: `var/dev/state/driver_state.json`

## 回滚

如果 post-merge sanity 检测到 main 红，driver 会：
1. Reopen issue #{{ISSUE_NUMBER}}
2. 在 issue 评论 "auto-merged but main broke; manual revert needed"
3. 计入 `consecutive_failures`（达 3 全局 halt）

人工回滚：`git revert <merge-sha> -m 1 && git push origin main`。

---

🤖 Generated by autonomous-bootstrap-driver. Manual review optional but encouraged.
